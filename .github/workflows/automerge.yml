name: Dependabot auto-merge

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: metanull/viewer-workflows/.github/workflows/dependabot-automerge.yml@v1.5.1
